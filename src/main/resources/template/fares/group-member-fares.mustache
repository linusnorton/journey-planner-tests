@fares @pending
Feature: Group member fares fares
  A clustered fare should be overridden by a fare to an NLC

  {{#scenarios}}
  Scenario: Clustered fare overridden by an NLC flow between {{origin}} and {{destination}}
    Given a query between "{{origin}}" and "{{destination}}" on "{{date}}" at "10:00"
    Then I should see a fare "{{ticketCode}}" on "{{groupRouteCode}}" that is "{{groupPrice}}" pence
    And I should see a fare "{{ticketCode}}" on "{{memberRouteCode}}" that is "{{memberPrice}}" pence

  {{/scenarios}}
